spring.application.name=keep


# Postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/keep
spring.datasource.username=user
spring.datasource.password=password
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mongo
spring.mongodb.host=localhost
spring.mongodb.port=27017
spring.mongodb.database=keep
spring.mongodb.username=root
spring.mongodb.password=password
spring.mongodb.authentication-database=admin

server.servlet.context-path=/api

spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

jwt.signing.key=c2FtcGxlSldUU2lnbmluZ0tleUZvclRlc3RpbmcxMjUWEBT=

system.user.email=SYSTEM

# CORS
#http.cors.headers.allowed-origin=${ALLOWED_ORIGIN}
http.cors.headers.allowed-origin=http://localhost:4200